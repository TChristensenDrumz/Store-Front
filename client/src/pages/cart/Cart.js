import React from 'react';
import CartItem from '../../components/CartItem';

function Cart() {
    return (
        <div className="container">
            <CartItem />
            <CartItem />
            <CartItem />
        </div>
    )
}

export default Cart
